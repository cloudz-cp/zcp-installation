image:
  kubectl:
    repository: registry.au-syd.bluemix.net/cloudzcp/k8s-kubectl
  s3:
    repository: registry.au-syd.bluemix.net/cloudzcp/aws-cli

# CHANGE
env:
  AWS_ACCESS_KEY_ID: BkX8mwoleNytLF0b4AcG
  AWS_SECRET_ACCESS_KEY: 3YmPiOgPEhbepuzpZ1S5pL0D7JKryxrKMQCZwVKt
  S3_ENDPOINT: https://s3.seo.ap.cloud-object-storage.appdomain.cloud
  S3_BUCKET: zcp-backup-coinery-mainnet
  GITEA_DEPLOY: zcp-git-gitea

jenkins:
  schedule: 5 */6 * * *  # CHANGE
  #successfulJobsHistoryLimit:
  #failedJobsHistoryLimit:
  serviceAccount: zcp-system-sa-cloudzcp-admin  # CHANGE

gitea:
  schedule: 5 */6 * * *  # CHANGE
  #successfulJobsHistoryLimit:
  #failedJobsHistoryLimit:
  serviceAccount: zcp-system-sa-cloudzcp-admin  # CHANGE

resources:
  requests:
    cpu: 100m
    memory: 128Mi

tolerations:
- effect: NoSchedule
  key: management
  operator: Equal
  value: "true"
affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
      - matchExpressions:
        - key: beta.kubernetes.io/arch
          operator: In
          values:
          - amd64
        - key: role
          operator: In
          values:
          - management
